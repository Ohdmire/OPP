# GitHub 发布检查清单

## 仓库内容

- [x] README 与当前版本功能一致。
- [x] 需求分析已放入 `docs/`。
- [x] `.gitignore` 覆盖 Node、Rust、Tauri、安装包、日志与本地密钥。
- [x] `package.json`、`Cargo.toml` 与 `tauri.conf.json` 版本一致。
- [x] 未在源码中发现硬编码的真实 Client Secret、Token 或用户目录。
- [ ] 选择并添加 `LICENSE`。
- [ ] 添加 2–4 张不包含私人数据的界面截图。

## 首次提交前

```powershell
git init
git add .
git status
git diff --cached --stat
```

确认暂存区中不包含：

- `node_modules/`
- `dist/`
- `src-tauri/target/`
- `.env*`
- OAuth 凭据或 Token
- 本地 osu! 文件、索引与缓存
- 编译生成的 `.exe` 或安装包

## 质量检查

```powershell
pnpm install --frozen-lockfile
pnpm lint
pnpm test
pnpm build

cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml

pnpm tauri build
```

## GitHub 仓库设置

- [ ] 填写仓库描述与 Topics：`osu`, `osu-game`, `tauri`, `rust`, `react`, `local-first`。
- [ ] 根据项目定位选择 Public 或 Private。
- [ ] 启用 Issues。
- [ ] 如接受外部贡献，补充贡献指南与行为准则。
- [ ] 如公开源码，启用 GitHub Private Vulnerability Reporting。
- [ ] 配置默认分支保护和必要检查。

## Release

- [ ] 创建 `v0.2.3` Tag。
- [ ] Release 标题使用 `OPP v0.2.3`。
- [ ] 附加 `OPP_0.2.3_x64-setup.exe`，不要把安装包提交到 Git 历史。
- [ ] 写明 Windows 与 WebView2 要求。
- [ ] 写明用户需要创建自己的 osu! OAuth 应用。
- [ ] 写明升级后是否需要重新扫描本地索引。
- [ ] 在无开发环境的 Windows 设备上安装、启动、登录和卸载一次。
- [ ] 如安装包未签名，在 Release Notes 中说明 Windows SmartScreen 可能出现提示。

## 隐私与合规

- [ ] 截图隐藏用户名、用户 ID、Token、Client ID 与本地绝对路径。
- [ ] 不随 Release 打包本机缓存、索引或 osu! 资源。
- [ ] 第三方音乐、谱面镜像或 AI 服务上线前确认授权与使用条款。
- [ ] README 保留“非官方社区项目”声明。
